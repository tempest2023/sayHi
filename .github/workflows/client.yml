name: Client CI

on:
  push:
    branches:
      - main
      - master
    paths:
      - 'client/**'
  pull_request:
    branches:
      - main
      - master
    paths:
      - 'client/**'

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'

    - name: Install dependencies
      run: |
        cd client
        npm install

    - name: Lint code
      run: |
        cd client
        npm run lint